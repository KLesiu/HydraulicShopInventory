<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%- include('style_connects') %>
    <title><%= title %></title>
</head>
<body>
    <%- include('layout') %>
    <div class="container">
        <h2><%= title %></h2>
        <% if(muff_list){
            %>  
            <ul>
                <% for(muff of muff_list){
                    %> 
                    <% if(muff.availability === true){
                        %> <li style="background-color: rgb(81, 182, 22)"><a href="<%= muff.url %>"><%= muff.name %></a></li> <%
                    }else{
                        %><li style="background-color: rgb(197, 11, 11)"><a href="<%= muff.url %>"><%= muff.name %></a></li> <%
                    } %>
                    <ul>
                        
                        <li><strong>Type:</strong> <%= muff.type  %></li>
                        
                    </ul>
                    <%
                } %>
            </ul>
            
            <%
        } 
        else{
            %>
            List doesnt have any product
            <%
        }
        
        %>
        <a href="muffs/create"><button class="addNew">ADD NEW</button></a>
    </div>
</body>
</html>