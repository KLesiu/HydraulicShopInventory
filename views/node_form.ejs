<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%- include('style_connects') %>
    <title><%= title %></title>
</head>
<body>
    <%- include('layout') %>
    <div class="container">
        <form action="" method="POST">
            <h1>FORM</h1>
            <label for="name">Name:</label>
            <input placeholder="Kolanko" name="name" type="text" value="<% if(update){%> <%= node.name %> <%} %>" >
            <label for="type">Type:</label>
            <input placeholder="45*" type="text" name="type" value="<% if(update){%> <%= node.type %>  <%} %>">
            <label for="material">Material:</label>
            <input placeholder="PP" type="text" name="material" value="<% if(update){%> <%= node.material %> <%} %>">
            <label for="price">Price: <% if(update&&node.price){%> (Current: <%if(node.price) %>   <%= node.price %>) <%} %></label>
            <input placeholder="30.57"type="number" step="0.01" name="price">
            <label for="fi">Fi:</label>
            <input name="fi" type="text" placeholder="75" value="<% if(update){
                %> <%= node.fi %> <%
            } %>">
            <label for="quantity">Quantity: <% if(update&&node.quantity){%> (Current: <%= node.quantity %>) <%}%></label>
            <input placeholder="72" type="number" name="quantity">
            <button type="submit"><% if(update){%> UPDATE <%} else{%>ADD<% } %></button>
        </form>
    <% if(errors){
        %>
        
        <ul class="errors">
              
            <% for(error in errors){
                %> 
                <li><%= (errors[error].msg) %></li>
                
                <%
            } %>
        </ul>
        <%
    } %>
    </div>
    
</body>
</html>