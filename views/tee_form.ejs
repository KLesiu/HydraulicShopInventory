<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%- include('style_connects') %>
    <title><%= title %></title>
</head>
<body>
    <%- include('layout') %>
    <div class="container">
        <form action="" method="POST">
            <h1>FORM</h1>
            <label for="name">Name:</label>
            <input placeholder="Trójnik" name="name" type="text" value="<% if(update){%> <%= tee.name %> <%} %>" >
       
            <label for="material">Material:</label>
            <input placeholder="PP" type="text" name="material" value="<% if(update){%> <%= tee.material %> <%} %>">
            <label for="price">Price: <% if(update&&tee.price){%> (Current: <%if(tee.price) %>   <%= tee.price %>) <%} %></label>
            <input placeholder="19.88" type="number" step="0.01" name="price">
            <label for="size">Size:</label>
            <input type="text" name="size" placeholder="63" value="<% if(update){%>
                <%= tee.size %>
                <%} %>">
            <label for="quantity">Quantity: <% if(update&&tee.quantity){%> (Current: <%= tee.quantity %>) <%}%></label>
            <input placeholder="11" type="number" name="quantity">
            <button type="submit"><% if(update){%> UPDATE <%} else{%>ADD<% } %></button>
        </form>
    <% if(errors){
        %>
        
        <ul class="errors">
              
            <% for(error in errors){
                %> 
                <li><%= (errors[error].msg) %></li>
                
                <%
            } %>
        </ul>
        <%
    } %>
    </div>
    
</body>
</html>