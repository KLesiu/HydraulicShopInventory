<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%- include('style_connects') %>
    <title><%= title %></title>
</head>
<body>
    <%- include('layout') %>
    <div class="container">
        <form action="" method="POST">
            <h1>FORM</h1>
            <label for="name">Name:</label>
            <input placeholder="Uchwyt-obejma" name="name" value="<% if(update){%> <%= chuck.name %> <%} %>" type="text" >
            <label for="material">Material:</label>
            <input placeholder="Guma" type="text" value="<% if(update){%> <%= chuck.material %> <%} %>" name="material">
            <label for="price">Price: <% if(update&&chuck.price){%> (Current: <%if(chuck.price) %>   <%= chuck.price %>) <%} %></label>
            <input placeholder="5.5" type="number" step="0.01"  name="price">
            <label for="size">Size:</label>
            <input placeholder="2" type="text" value="<% if(update){%> <%= chuck.size %> <%} %>" name="size">
            <label for="r">R:</label>
            <input placeholder="59-63" type="text"  value="<% if(update){%> <%= chuck.r %> <%} %>" name="r">
           
            <label for="quantity">Quantity: <% if(update&&chuck.quantity){%> (Current: <%= chuck.quantity %>) <%}%></label>
            <input placeholder="72" type="number" name="quantity">
            <button type="submit"><% if(update){%> UPDATE <%} else{%>ADD<% } %></button>
        </form>
    <% if(errors){
        %>
        
        <ul class="errors">
              
            <% for(error in errors){
                %> 
                <li><%= (errors[error].msg) %></li>
                
                <%
            } %>
        </ul>
        <%
    } %>
    </div>
    
</body>
</html>