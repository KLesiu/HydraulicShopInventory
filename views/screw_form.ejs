<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%- include('style_connects') %>
    <title><%= title %></title>
</head>
<body>
    <%- include('layout') %>
    <div class="container">
        <form action="" method="POST">
            <h1>FORM</h1>
            <label for="name">Name:</label>
            <input placeholder="Śrubunek GZ" name="name" type="text" value="<% if(update){%> <%= screw.name %> <%} %>" >
            <label for="type">Type:</label>
            <input placeholder="klasyczny,holender,mufowy" type="text" name="type" value="<% if(update){%> <%=screw.type %> <%} %>">
            <label for="material">Material:</label>
            <input placeholder="PP" type="text" name="material" value="<% if(update){%> <%= screw.material %> <%} %>">
            <label for="price">Price: <% if(update&&screw.price){%> (Current: <%if(screw.price) %>   <%= screw.price %>) <%} %></label>
            <input placeholder="36.32" type="number" step="0.01" name="price">
            <label for="size">Size:</label>
            <input type="text" placeholder="25x3/4" name="size" value="<% if(update){%> <%= screw.size %> <%} %>">
            <label for="quantity">Quantity: <% if(update&&screw.quantity){%> (Current: <%= screw.quantity %>) <%}%></label>
            <input placeholder="10" type="number" name="quantity">
            <button type="submit"><% if(update){%> UPDATE <%} else{%>ADD<% } %></button>
        </form>
    <% if(errors){
        %>
        
        <ul class="errors">
              
            <% for(error in errors){
                %> 
                <li><%= (errors[error].msg) %></li>
                
                <%
            } %>
        </ul>
        <%
    } %>
    </div>
    
</body>
</html>